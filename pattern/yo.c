#include<stdio.h>
#include<stdlib.h>
#include<math.h>
#include<string.h>

int main(){
    char str[20];
    // printf("input : ");
    // scanf("%s",&str);
    // printf("output : %s\n",str);
      puts("input : ");
    gets(str);
    puts("output : ");
    puts(str);

    return 0;
}